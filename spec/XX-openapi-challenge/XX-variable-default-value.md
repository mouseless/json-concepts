# Default Value

```json
{
    "required?": "$required:boolean(true)",
    "server?": [
        {
            "url":"$url:url",
            "@default": {
                "url": "/"
            }
        }
    ]
}
```

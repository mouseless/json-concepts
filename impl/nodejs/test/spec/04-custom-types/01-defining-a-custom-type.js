describe('spec/custom-types/defining-a-custom-type', function() {
    it('should parse custom type and include it in shadow');

    describe('specifying a base type', function() {
        it('should use base type of custom type when validating values');
        it('should use custom type name in validation message');
        it('should use any type when no base type is specified');
    });
});
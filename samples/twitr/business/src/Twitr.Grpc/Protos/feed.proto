syntax = "proto3";

option csharp_namespace = "Twitr.Grpc";

package feed;

service FeedGrpc {
  rpc GetPosts (PostQuery) returns (stream Post);
}

message PostQuery {

}

message Post {
  string uid = 1;
  string user = 2;
  string content = 3;
}

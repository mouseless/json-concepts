# Literals

> TBD

`string` is a value literal.

`service.concepts.json`

```json
{
    "$service+": {
        "$parameter*": "string"
    }
}
```

`returns` is a key literal.

`service.concepts.json`

```json
{
    "$service+": {
        "$parameter*": "string",
        "returns": "string"
    }
}
```
